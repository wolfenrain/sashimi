name: example
description: A basic example of the Sashimi engine
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flame: ^1.5.0
  flutter:
    sdk: flutter
  sashimi:
    path: ../

dev_dependencies:
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
